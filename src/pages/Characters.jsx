import React from 'react';
import Slider from './../components/Slider';

const Characters = () => {
  return (
    <div style={{ color: '#fff' }}>
      {/*find figure data in Slider*/}
      <Slider />
    </div>
  );
};

export default Characters;
